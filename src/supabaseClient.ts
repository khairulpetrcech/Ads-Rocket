
// This file is no longer used after reverting to LocalStorage mode.
// Keeping empty exports to prevent build errors if imports linger temporarily.
export const supabase = null;
export const encryptKey = (t: string) => t;
export const decryptKey = (t: string) => t;